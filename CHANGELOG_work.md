# CHANGELOG — Aibrium build log

Claude Code appends one entry at the end of every task. Newest at the bottom.

**Entry format:**
```
## [Phase N] Short task name — YYYY-MM-DD
- files: path/one.tsx, path/two.ts
- notes: what was built / anything worth knowing
- status: ⏳ built, untested        ← set on completion
```
When the task has been run (`npm run dev`) and works, change the status line to:
```
- status: ✅ verified — YYYY-MM-DD
```

Status legend: `⏳ built, untested` · `✅ verified` · `⚠️ needs fix (reason)`

---

<!-- entries below this line -->

## [Phase 1] Scaffold + design system foundation — 2026-07-21
- files: web/ (create-next-app scaffold), web/vercel.json, web/next.config.mjs, web/tailwind.config.ts, web/src/app/globals.css, web/src/app/layout.tsx, web/src/app/page.tsx, web/src/lib/utils.ts, web/src/lib/calendly.ts, web/src/lib/catalogues.ts, web/src/lib/motion.ts
- notes: Scaffolded Next.js 14.2 (TS, ESLint, Tailwind, src-dir, App Router, @/* alias) into /web; installed framer-motion, lucide-react, clsx, tailwind-merge. vercel.json = nextjs framework. globals.css: palette CSS vars + prefers-reduced-motion block. tailwind.config.ts: brand colors, display/body fontFamily, maxWidth.content, card/float shadows, btn radius. next.config.mjs: placehold.co remotePattern. layout.tsx: Cormorant Garamond 500/600 + Inter 400/500 via next/font/google with display swap. lib: cn(), CALENDLY_URL + openCalendlyPopup, 4 catalogues + hero with placehold.co URLs, fadeInUp/stagger variants. Minimal page.tsx with H1 placeholder to confirm fonts + colors. No Header/Footer/sections yet (later phases).
- verify: npm run dev → GET / returns 200, H1 renders, text-gold class present, both next/font variable classes on <html>, tsc --noEmit clean.
- status: ✅ verified — 2026-07-21

## [Phase 1] Shell — Header, Footer, UI primitives — 2026-07-21
- files: web/src/components/ui/Button.tsx, web/src/components/ui/SectionLabel.tsx, web/src/components/ui/Card.tsx, web/src/components/ui/CalendlyButton.tsx, web/src/components/layout/Header.tsx, web/src/components/layout/Footer.tsx, web/src/app/layout.tsx, web/src/app/page.tsx
- notes: Button (primary/secondary, onDark, md/lg sizes, href→<a> else <button>; primary gold/ink→gold-hover, secondary outline light=ink / dark=cream per spec). SectionLabel (gold small-caps, tracking 0.22em via CSS, optional 24px gold hairline — no typed spaces). Card (base: white bg, hairline border, radius-md, shadow-card; interactive adds group + hover:border-gold for portfolio/pricing). CalendlyButton (wraps Button, onClick=openCalendlyPopup). Header (fixed slim; transparent+cream text over future dark hero, solidifies to cream bg+ink text+hairline shadow past 40px scroll, 0.3s; desktop nav Work/Process/Pricing/FAQ anchors + Book a Call; mobile hamburger slide-in menu). Footer (ink bg, cream text, 3 cols: wordmark+tagline / General+Production mailtos / nav+Book a call Calendly; bottom legal line with Privacy→/privacy). Wired Header + Footer around {children} in layout.tsx. Added id="top" to page main for wordmark anchor.
- verify: npm run dev → GET / returns 200; wordmark "Aibrium.", "Book a Call", office@aibriumstudio.com, "Privacy Policy", and legal line all render; no dev errors; tsc --noEmit clean.
- status: ✅ verified — 2026-07-21

## [Phase 2] Hero (dark) + Problem→Fix — 2026-07-21
- files: web/src/components/sections/Hero.tsx, web/src/components/sections/ProblemFix.tsx, web/src/app/page.tsx
- notes: Hero — ink bg with warm radial (#242018→#1A1A1A) + blurred gold bloom rgba(138,109,59,0.28) behind the hero image; split layout (text left / image right on md+, image above text on mobile). Single page <h1> "Your brand's own models. New visuals every week." (Cormorant 600, cream), gold SectionLabel, #B9B2A6 sub, primary CalendlyButton "Book a 15-min Call" + secondary onDark "See the Work" → #work. Two floating info-cards (shadow-float, true facts: DELIVERY / YOUR MODEL with A avatar dot) shown md+ only, hidden on mobile. On-load stagger fade-up (initial→animate) on label/H1/sub/buttons. Hero carries id="top" for the header wordmark anchor. ProblemFix — cream section, clamp vertical padding, gold "Why We Exist" label + hairline, H2 "Great visuals shouldn't require a production.", body, three fact cards (Weekly delivery / Models exclusive / 3 ad-ready formats) via Card; whileInView stagger fade-up. page.tsx now renders <Hero/> + <ProblemFix/> (removed placeholder H1).
- verify: npm run dev → GET / 200; hero H1, "Book a 15-min Call", "See the Work", both floating cards ("Finished visuals every Friday" / "never reused"), "Why We Exist", H2, all three fact cards, and placehold.co hero image all render; exactly one <h1>; tsc --noEmit clean; no dev errors.
- fix: placehold.co returns SVG by default, which the Next image optimizer rejects ("received null"). Appended .png to every placehold.co URL (catalogues.ts dark() + HERO_IMAGE) so they return raster PNGs, and added `unoptimized` (with TODO to remove once real /public images land) to the Hero <Image> as a safety net. Re-ran npm run dev: hero src is the .png URL, serves content-type image/png 200, no "received null" / image errors.
- status: ✅ verified — 2026-07-21

## [Phase 3] The Work (portfolio + lightbox) + Exclusive models — 2026-07-21
- files: web/src/components/sections/Work.tsx, web/src/components/ui/Lightbox.tsx, web/src/components/sections/ExclusiveModels.tsx, web/src/app/page.tsx
- notes: Confirmed catalogues.ts (4 catalogues, each .png cover + 8 placeholder images). Work (id="work") — white section, gold "The Work" label + hairline, H2 "Judge us by the images.", 2×2 catalogue grid (1 col mobile) of interactive Cards: cover (unoptimized <Image>) with group-hover scale-1.03 zoom + hairline→gold, name (Cormorant) + category (grey caption); each card is a <button> that opens the Lightbox; caption line "Sample collections … written permission." below the grid. Commented-out empty FUTURE PROOF SLOT between Block 4 and Block 5 (logos/testimonials/case study) — no fabricated proof. Lightbox — full-screen fixed modal, ink/90 backdrop, AnimatePresence fade only, prev/next chevrons (wraparound), close via Esc + click-outside + X (stopPropagation on image/arrows), ArrowLeft/Right keyboard nav, body-scroll lock + focus moved to close button (focus trap), fires window.plausible('Lightbox Open', { props: { catalogue }}) on open. ExclusiveModels — cream section, image-forward showcase: casting-board visual (2×2 candidate thumbnail grid, unoptimized placeholders) + text (gold "Your Models" label, H2 "Cast once. Yours forever.", body), three-step row (We cast / You choose / We lock). page.tsx renders Hero → ProblemFix → Work → ExclusiveModels.
- verify: npm run dev → GET / 200; "The Work"/H2, all 4 catalogue names (SOLÈNE/KAIA SWIM/MOVA ACTIVE/VELA SKIN), caption line, id="work", "Your Models"/H2 "Cast once. Yours forever.", three steps (We cast/You choose/We lock), and casting-board Candidate placeholders all render; exactly one <h1>; tsc --noEmit clean; no "received null"/compile errors. (FUTURE PROOF SLOT is a JSX comment → correctly absent from rendered HTML.)
- status: ✅ verified — 2026-07-21

## [Phase 4] Process + Pricing + FAQ — 2026-07-21
- files: web/src/components/sections/Process.tsx, web/src/components/sections/Pricing.tsx, web/src/components/sections/Faq.tsx, web/src/app/page.tsx
- notes: Process (id="process") — white section, gold "The Process" label, H2 "From signing to your first delivery in two weeks.", four numbered Cards 01–04 (gold Cormorant numbers): Casting & Style Lock / Submit Mondays / Delivered Fridays / Refine. Pricing (id="pricing") — cream section, gold "Pricing" label, H2 "Start with a pilot. Scale when it works."; featured PILOT card (gold border, first, prominent) "$900" + START WITH THE PILOT CalendlyButton → popup; three tier cards (CORE $3,900 / ENGINE $5,900 highlighted with gold border + raised + "Most Popular" gold tag / FULL STUDIO $8,900) with feature lists; line under cards "Quarterly prepay −8% · 3-month minimum · … 1:1, 4:5, and 9:16." Faq (id="faq") — white section, gold "FAQ" label, all 7 Q&As verbatim, accordion all closed by default (one open at a time), AnimatePresence height 0→auto transition, Plus icon rotates 45° when open. "AI" appears only in Q1. page.tsx order: Hero → ProblemFix → Work → ExclusiveModels → Process → Pricing → Faq.
- verify: npm run dev → GET / 200; id="process"/"process" heading + all 4 step titles, id="pricing" + "THE PILOT"/$900/"Start with the Pilot"/"Most Popular"/FULL STUDIO/"3-month minimum", id="faq" + Q1/Q7 questions all render; exactly one <h1>; tsc --noEmit clean; no compile errors. AI-constraint: `grep -rnoE "\bAI\b" src/` returns exactly one match — Faq.tsx line 13 (Q1 answer) — and nowhere else in source.
- status: ✅ verified — 2026-07-21

## [Phase 5] Final CTA (inline Calendly) + /privacy — 2026-07-21
- files: web/src/components/ui/CalendlyInline.tsx, web/src/components/sections/FinalCta.tsx, web/src/app/page.tsx, web/src/app/privacy/page.tsx
- notes: CalendlyInline — .calendly-inline-widget div with data-url={CALENDLY_URL}, minWidth 320 / height 680; auto-initialized by the widget script loaded in layout.tsx. FinalCta — calm cream closing section (clamp padding), gold "Next Step" label + hairline, H2 "Fifteen minutes. Bring a product.", body, inline Calendly widget, then email line "Prefer email? hello@aibrium.com — we reply within 24 hours, Monday to Friday." (mailto, gold underline). Added as last section before footer in page.tsx (Hero → … → Faq → FinalCta). /privacy — app/privacy/page.tsx: single <article> on cream, max-w-760px, top padding to clear fixed header, page metadata export. Realistic (non-Lorem) placeholder legal text: intro + What we collect (cookieless analytics + info you send) / How we use it / Third parties (Plausible + Calendly) / Your rights / Contact (hello@aibrium.com mailto + legal entity + Chișinău). "Last updated: 2026". Its own <h1> "Privacy Policy" (per-page h1 rule). Header + Footer come from shared layout.tsx.
- verify: npm run dev → GET / 200 and GET /privacy 200. Home: "Next Step"/H2 "Fifteen minutes. Bring a product.", .calendly-inline-widget with data-url="https://calendly.com/PLACEHOLDER/15min", "Prefer email"/hello@aibrium.com all render; exactly one <h1>. Privacy: "Privacy Policy"/"Last updated: 2026"/cookieless/Plausible/Calendly/"Your rights"/hello@aibrium.com render, shared Header ("Aibrium.", "Book a Call") + Footer present, exactly one <h1>. tsc --noEmit clean; no compile errors.
- status: ✅ verified — 2026-07-21

## [Phase 6] Motion pass (subtle) — 2026-07-21
- files: web/src/components/MotionProvider.tsx (new), web/src/app/layout.tsx
- notes: Audit + reduced-motion guard. Confirmed all 8 sections already animate via fadeInUp: 7 use whileInView + viewport={{ once:true, margin:'-80px' }} (ProblemFix/Work/ExclusiveModels/Process/Pricing/Faq/FinalCta), Hero uses initial→animate on-load stagger — no missing sections. Confirmed the other required transitions exist: portfolio cover group-hover scale-1.03 (CSS), header solidify 0.3s (CSS), FAQ AnimatePresence height 0→auto, Lightbox AnimatePresence fade-only. Gimmick sweep (parallax/autoplay/useScroll/useTransform/useSpring/counter) → none found. NEW: added MotionProvider (client) wrapping the app in layout.tsx with <MotionConfig reducedMotion="user"> — the JS-side counterpart to the globals.css prefers-reduced-motion block: Framer's rAF-driven transform/movement is disabled for users who request reduced motion (CSS block already zeroes CSS transitions incl. hover zoom + header solidify). Opacity-only remains, so content can never be stuck hidden.
- verify: npm run dev → GET / 200, GET /privacy 200; home + privacy each render exactly one <h1>; SSR content ("New visuals every week", "Judge us by the images", "Fifteen minutes") present so opacity-based motion never hides content; tsc --noEmit clean; no compile errors.
- status: ✅ verified — 2026-07-21

## [Phase 7] SEO, analytics, performance QA — 2026-07-21
- files: web/src/app/layout.tsx, web/src/app/sitemap.ts (new), web/src/app/robots.ts (new), web/src/app/icon.tsx (new), web/src/app/favicon.ico (removed), web/src/types/global.d.ts (new), web/src/lib/analytics.ts (new), web/src/components/CalendlyEvents.tsx (new), web/src/lib/calendly.ts, web/src/components/ui/Lightbox.tsx, web/src/components/layout/Footer.tsx, web/src/components/sections/FinalCta.tsx
- notes: SEO — full metadata in layout.tsx (spec title/description, metadataBase https://aibrium.com, canonical, OpenGraph incl. /og.jpg + siteName + type). Heading audit: home has exactly one <h1> (Hero), all sections <h2>, cards <h3>; privacy has its own single <h1> (per-page rule). sitemap.ts (/ priority 1, /privacy 0.3) + robots.ts (allow all, sitemap + host). Favicon — removed default favicon.ico, added app/icon.tsx generating gold "A." on ink via next/og ImageResponse. Analytics — Plausible tagged-events script (deferred, lazyOnload, data-domain aibrium.com) + Calendly widget.js (lazyOnload) + Calendly widget.css/preconnect in <head>. Custom events: Calendly Popup (existing), Lightbox Open (existing), NEW Mailto Click (lib/analytics.trackMailtoClick wired on footer hello@/office@ + FinalCta hello@), NEW Booking (CalendlyEvents client listener on window 'message' → calendly.event_scheduled, origin-checked). Added src/types/global.d.ts typing window.plausible + window.Calendly and removed the scattered @ts-expect-error in calendly.ts + Lightbox.tsx. Perf/responsive: no raw <img> — all 4 <Image> have width/height/alt; scripts deferred; fonts next/font swap. og.jpg + public/ assets still pending (placeholders) — flagged.
- verify: npm run dev → / /privacy /sitemap.xml /robots.txt all 200, /icon 200 image/png. Home <head> has rel=canonical, og:title, og:image, rel=icon, plausible tagged-events script, calendly widget.js + widget.css. Home exactly one <h1>. sitemap lists https://aibrium.com/ + /privacy; robots allows all + sitemap/host. `npm run build` succeeds: 8 routes prerendered static (incl. /icon, /robots.txt, /sitemap.xml); First Load JS home 151 kB / privacy 87.5 kB. tsc --noEmit clean; no build/compile errors.
- caveats: (1) Lighthouse NOT run — no headless Chrome in this env; can't report the 90+/95+ numbers. Perf-relevant measures are in place (deferred third-party scripts, next/font swap, next/image w/h/alt, static prerender) but the score must be confirmed on a real run. (2) /og.jpg referenced in metadata but the file doesn't exist yet (no public/ dir) → OG image 404s until the real hero asset is dropped in /public. (3) privacy page mailto is untracked (server component; would need a client wrapper) — footer + FinalCta mailtos are tracked.
- status: ✅ verified — 2026-07-21

## [Deviation] Hero mobile — hide visual, keep facts — 2026-07-23
- files: web/src/components/sections/Hero.tsx, SKILL.md (Block 2 mobile line)
- notes: APPROVED deviation from Block 2 ("image above text; cards hidden"). (1) Two-column split moved md:→lg: so lg+ desktop stays pixel-identical while md–lg no longer shows an empty column. (2) Reordered DOM to Text-first / Visual-second and dropped order-* utilities — natural grid flow keeps text-left/visual-right on desktop, and puts the visual after text in source order. (3) Visual block (bloom + image + both floating cards) is `hidden lg:block`. (4) Removed `priority` from the hero <Image> so display:none + default lazy loading means mobile never fetches/preloads the hero placeholder. (5) New mobile-only facts block (lg:hidden) under the CTAs inside the same staggered motion container: SectionLabel gold small-caps label + body-text cream value — DELIVERY→"Finished visuals every Friday", YOUR MODEL→"Exclusive to your brand — never reused"; no new colors/components. (6) On-load stagger intact on both breakpoints; reduced-motion still handled by MotionConfig + CSS block. Re-enabling the visual on mobile later = change `hidden lg:block`→`block` (shows below text, no markup rewrite). Trade-off: hero image no longer preloaded as desktop LCP (removed priority) — flagged. SKILL.md Block 2 mobile line updated to record the deviation.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>. Rendered HTML: both facts appear exactly 2× (mobile lg:hidden block + desktop floating card); mobile facts container `mt-10 flex flex-col gap-5 lg:hidden` present; visual wrapper `relative hidden lg:block`; Hero grid `lg:grid-cols-2` (no md:grid-cols-2 on Hero — the md:grid-cols-2 in HTML is Work's portfolio grid); DOM order text→CTA→mobile-facts→visual confirmed via position check; NO <link rel=preload> for the hero image (only webpack.js script preload) → priority removal confirmed, mobile won't fetch it. Not checkable here: pixel-diff of desktop and live mobile paint (no browser/Lighthouse) — desktop parity is by construction (lg: values == prior md: values; DOM reorder yields identical column placement).
- status: ✅ verified — 2026-07-23

## [Enhancement] Lightbox touch-swipe navigation — 2026-07-23
- files: web/src/components/ui/Lightbox.tsx
- notes: Added Framer Motion drag (no new lib) to the image container. Swipe left→next, right→prev, down→close; thresholds SWIPE_DISTANCE 80px OR SWIPE_VELOCITY 500px/s, else dragSnapToOrigin springs it back (dragElastic 0.18). dragDirectionLock + an |offset.x| vs |offset.y| dominant-axis check keeps horizontal (navigate) and vertical (close) from fighting; swipe up just snaps back. Existing behavior preserved unchanged: arrow buttons (all breakpoints), X, Esc, ArrowLeft/Right keys, body-scroll lock, Plausible open event, focus-to-close. Looping preserved (goPrev/goNext already wrap via modulo) so swipe wraps like the arrows — no resistance-at-ends added because it already loops (per req 5's "unless it already does"). Reduced motion: useReducedMotion → snap transition {duration:0} + dragMomentum off, so the gesture/navigation still works with no snap animation (MotionConfig also active globally). touch-action:none on the draggable + existing body overflow hidden prevent the page behind from scrolling during a swipe on touch. No visual additions (no dots/hints/cursor/counter — the existing "n / N" counter kept as-is); Image gets draggable={false} to kill the native ghost-drag on desktop mouse. Note: SKILL "Lightbox: fade only" intentionally superseded per this explicit request — only the user-driven drag moves; the image swap stays fade/instant.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>, catalogue open buttons render, Lightbox module compiles with no errors (drag/PanInfo/useReducedMotion types valid). Not exercisable headlessly: the actual touch-swipe gesture, snap-back, and reduced-motion snap — these need a real touch device / browser devtools touch emulation to confirm visually.
- status: ✅ verified — 2026-07-23

## [Assets] Work — real client photography replaces placeholders — 2026-07-23
- files: web/public/work/{solene,kaia-swim,mova-active,vela-skin}/*.jpg (71 new), web/src/lib/catalogues.ts (restructured), web/src/components/sections/Work.tsx, web/src/components/ui/Lightbox.tsx, web/_incoming/ (deleted)
- notes: Slugs confirmed identical to folder names (solene/kaia-swim/mova-active/vela-skin) — none renamed. Moved 71 staged files from _incoming/ to public/work/<slug>/, renamed 01.jpg.. by the parenthetical (n) ASCENDING (not alphabetical). kaia-swim had a (0) file "KAIA SWIM — Azure Coast Collection(0).jpg" (formatted unlike its 23 siblings) — per instruction stopped and asked; user chose "First → 01.jpg", so (0)→01.jpg and (1..23)→02..24.jpg (24 total). Counts: solene 12, kaia-swim 24, mova-active 17, vela-skin 18. Deleted _incoming/. catalogues.ts restructured: images/cover are now CatalogueImage objects { src, width, height, alt } (was string[]) so next/image has real dims and no CLS — all images measured at 1240×1754 via a no-library JPEG SOF parser. cover = each catalogue's 01.jpg (no separate-intent cover field existed). alt from SKILL Content facts descriptors: "<NAME> — <descriptor>, visual <n>" (elegant fashion / swimwear / activewear / beauty); cover alt "<NAME> — <descriptor>". Updated Work.tsx (cover.src/alt/width/height) + Lightbox.tsx (images[i].src/alt/width/height). Kept `unoptimized` on these <Image>s so Next serves the originals byte-for-byte (req 9: no resize/recompress/convert); width/height still prevent layout shift. Images NOT modified on disk.
- step 7 (remotePattern removal): DELIBERATELY NOT DONE — user decision "Leave it" (2026-07-24). placehold.co is still legitimately used by HERO_IMAGE (catalogues.ts) + ExclusiveModels casting board, for which no real assets were provided; removing the next.config.mjs remotePattern would break the hero + casting board at runtime. next.config.mjs left unchanged (remotePattern kept). Revisit when hero/casting-board real assets arrive: then drop those two placehold.co refs and remove the remotePattern. So placehold.co intentionally still present in exactly 3 spots: catalogues.ts HERO_IMAGE, ExclusiveModels.tsx candidates, next.config.mjs remotePattern.
- anomaly report (no files changed): [A] under 300KB → solene/01.jpg (64KB), kaia-swim/02.jpg (63KB), kaia-swim/14.jpg (84KB), vela-skin/02.jpg (201KB). [B] dimension outliers → none (all 1240×1754). [C] orientation inconsistency → none (all portrait).
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>; all 4 covers render local /work/<slug>/01.jpg; direct fetches of solene/01, kaia-swim/01+24, mova-active/17, vela-skin/18 all return 200 image/jpeg at exact original byte sizes (served as-is, unmodified); no dev errors / no "unconfigured host" (hero + casting board still work since remotePattern kept). Steps 1–6, 8, 9 complete + verified. Step 7 intentionally HELD pending user decision (see above).
- status: ✅ verified — 2026-07-23 (step 7 held)

## [Rework] Lightbox — native scroll-snap replaces Framer drag — 2026-07-24
- files: web/src/components/ui/Lightbox.tsx (full rewrite), web/src/app/globals.css (+.no-scrollbar utility)
- notes: Removed ALL Framer Motion drag (drag/dragDirectionLock/dragSnapToOrigin/dragElastic/onDragEnd/PanInfo/SWIPE_* thresholds/modulo-loop goPrev-goNext). New nav = native horizontal scroll-snap: overflow-x track (snap-x snap-mandatory) of full-width flex-none slides (snap-center). touch-pan-x + overscroll-contain on the track so vertical finger movement never drags the image or scrolls the page behind. Scrollbar hidden via new .no-scrollbar utility in globals.css (element stays scrollable). Active index synced from IntersectionObserver (root=track, threshold 0.6) → onIndexChange; no scroll-event math. Arrows + Left/Right keys call track.scrollTo({left:i*clientWidth, behavior}) with behavior smooth, or 'auto' (instant) under prefers-reduced-motion (useReducedMotion). No loop — index clamped, first/last stop. Visual: fully opaque bg-ink backdrop (no translucency), z-[100] above header z-50 so nothing behind shows incl. header; true fullscreen fixed inset-0 height:100dvh with h-screen (100vh) fallback; image object-contain vertically centred; counter now small uppercase tracking-0.22em gold (grey bar removed); arrows hidden below lg (hidden lg:block), kept desktop; close btn + counter respect env(safe-area-inset-*). Body scroll locked via position:fixed/top:-scrollY and exact scroll position restored on close (window.scrollTo). Esc + close button kept; click-outside-to-close dropped so a tap/swipe never closes accidentally. Loading: only active±1 slides loading="eager", rest lazy (req 17); kept unoptimized (photos byte-for-byte). No npm deps added; Work grid/cards/colours untouched (Lightbox props contract unchanged → Work.tsx not modified). Kept AnimatePresence+motion opacity-only fade (SKILL "Lightbox: fade only") — not drag.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>, Work grid still renders all 4 covers (untouched). Compiled CSS bundle contains every lightbox utility: .no-scrollbar (base + ::-webkit-scrollbar display:none), snap-x/snap-mandatory/snap-center, touch-pan-x, overscroll-contain, h-screen, z-index:100 (z-[100], above header z-50), bg-ink; 100dvh is an inline style applied on open. No drag logic remains (only draggable={false} native attr). No dev/compile errors. NOT exercisable headlessly: the actual scroll-snap swipe, IntersectionObserver index sync, arrow/keyboard scrollTo (smooth vs instant), opaque-backdrop/header-occlusion, safe-area insets, and scroll-position restore — these need a real browser / touch device (or DevTools device emulation) to confirm visually.
- status: ✅ verified — 2026-07-24

## [Config+Type] Real Calendly URL (literal) + EB Garamond/Montserrat/Inter typography — 2026-07-24
- files: web/src/lib/calendly.ts, web/src/app/layout.tsx, web/tailwind.config.ts, web/src/components/ui/SectionLabel.tsx, web/src/components/ui/Button.tsx, web/src/components/ui/Lightbox.tsx, web/src/components/sections/Pricing.tsx, web/src/components/sections/Hero.tsx, web/src/components/layout/Header.tsx, web/src/components/layout/Footer.tsx, web/src/app/privacy/page.tsx, SKILL.md (typography + Calendly sections)
- notes (Task 1): CALENDLY_URL default is now the literal 'https://calendly.com/hello-aibrium' with NEXT_PUBLIC_CALENDLY_URL as optional override (?? pattern → never undefined, zero-config). No .env.local existed (none created/deleted); .gitignore untouched. No stray booking URLs — other calendly.com strings are assets.calendly.com (widget) + the postMessage origin check in CalendlyEvents.tsx (both legit). CTAs verified all import from lib/calendly.ts: Header "Book a Call" desktop+mobile (CalendlyButton→openCalendlyPopup), Hero "Book a 15-min Call" (CalendlyButton), Pricing "Start with the Pilot" (CalendlyButton), Footer "Book a call" (openCalendlyPopup), Final CTA inline (CalendlyInline data-url={CALENDLY_URL}).
- notes (Task 2): 3-role type system. layout.tsx: replaced Cormorant_Garamond with EB_Garamond (500/600, --font-eb-garamond) + added Montserrat (500/600, --font-montserrat); Inter unchanged; all display:swap, latin subset, no <link>. tailwind tokens: display=EB Garamond, NEW label=Montserrat, body=Inter. font-label applied ONLY to label/nav/counter/micro-copy: SectionLabel, Button labels, Lightbox counter, Pricing "Most Popular", Hero 2 floating micro-labels, Footer General/Production labels + col-3 nav (links + Book a call), privacy "Legal", Header desktop+mobile nav. Body (Inter) untouched everywhere — no Montserrat in paragraphs (verified: 0 font-body uppercase lines remain). Cormorant fully removed (0 refs). Optical: added tracking-[-0.01em] to hero H1 only (EB Garamond wider than Cormorant); font-sizes + line-heights unchanged. SKILL.md typography + Calendly sections updated.
- caveat: req 11 optical check at 390/768/1440 is a visual task — can't render headlessly. Applied the one knowledge-based tightening (H1 -0.01em); H2s/line-heights left as-is pending your on-device look. If H2s read loose in EB Garamond, a matching -0.01em is the follow-up.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>; inline Calendly data-url="https://calendly.com/hello-aibrium" (real literal, no env needed); <html> carries 3 next/font variable classes; compiled CSS defines --font-eb-garamond + --font-montserrat + --font-inter and includes EB_Garamond/Montserrat/Inter families; zero Cormorant refs in src+CSS; 0 uppercase micro-copy lines left on font-body. No compile errors. Not verifiable headlessly: the actual rendered look of EB Garamond/Montserrat at 390/768/1440 (req 11 optical check).
- status: ✅ verified — 2026-07-24

## [Brand+UX] Gold #C8A96A + gold-deep + Back-to-close hook + opaque Calendly popup — 2026-07-24
- files: tailwind.config.ts, src/app/globals.css, src/app/icon.tsx, src/lib/catalogues.ts, src/components/ui/SectionLabel.tsx, src/components/sections/{Hero,Process,Faq,Pricing,FinalCta}.tsx, src/app/privacy/page.tsx, src/components/ui/Card.tsx, src/hooks/useBackToClose.ts (new), src/components/CalendlyProvider.tsx (new), src/components/ui/{Lightbox,CalendlyButton}.tsx, src/components/layout/{Header,Footer}.tsx, src/lib/calendly.ts, src/types/global.d.ts, src/app/layout.tsx
- notes TASK1 (gold): brand gold now #C8A96A (tailwind `gold` + --color-gold). gold-hover = #B48F43 (new gold darkened by the SAME HSL-lightness ratio ~0.807 the old #8A6D3B→#6F5730 pair used; hue+sat preserved). NEW `gold-deep` #8A6D3B (+ --color-gold-deep) for gold TEXT/HAIRLINES on cream/white (C8A96A fails contrast there): SectionLabel made onDark-aware (gold on dark, gold-deep on light) + onDark added to the 3 Hero SectionLabels; gold-deep applied to Process numbers, Faq Plus icon, Pricing $900 + both card borders (PILOT+ENGINE), Hero floating-card micro-labels (cream/white surfaces), FinalCta + privacy email underlines/hover, privacy "Legal", Card interactive hover border. Kept #C8A96A (gold) on dark/fills: Footer (ink), Lightbox (ink), Header wordmark (C8A96A on both per spec) + nav hover, Button bg-gold fill, Pricing "Most Popular" fill. Buttons: primary + "Most Popular" already use text-ink on the gold fill — readable on C8A96A, NO label-colour change needed. icon.tsx favicon 8A6D3B→C8A96A; catalogues.ts placeholder URL 8A6D3B→C8A96A. Grep confirms 8A6D3B remains ONLY in the gold-deep token defs; 6F5730 gone entirely. No other colour/spacing/layout changed.
- notes TASK2/3 (back button + Calendly): NEW useBackToClose(isOpen,onClose) hook — pushes ONE history entry (marker state, no URL/hash change) on open, popstate→onClose, consumes via history.back() on any other close; refs guard double-close + single-push (swiping never re-pushes); popstate listener cleaned on unmount. Lightbox uses it (open,onClose). Mobile menu had NO back handling → NEEDED THE FIX → added useBackToClose(menuOpen,closeMenu). Calendly popup: NEW CalendlyProvider owns open state + openPopup/closePopup via Calendly's real API (initPopupWidget/closePopupWidget — not element-hide, so no iframe leak), wired through useBackToClose; MutationObserver on .calendly-overlay removal catches Calendly's own X-close to keep state/history in sync. CTAs now call openPopup via useCalendly() (CalendlyButton + Footer); old stateful openCalendlyPopup removed from lib (split into initCalendlyPopup/closeCalendlyPopup). global.d.ts: added closePopupWidget. layout wraps app in CalendlyProvider. req17: mobile menu CTA passes onClick={()=>setMenuOpen(false)} so the menu closes before the popup opens (CalendlyButton runs onClick then openPopup). globals.css: .calendly-overlay → opaque var(--color-ink) + z-index 2147483000 (above header z-50, fixes header showing through/over); .calendly-popup-close → safe-area-inset padding. Calendly URL + CTA copy unchanged.
- verify: tsc --noEmit clean; npm run dev → GET / 200 + GET /privacy 200, one <h1>. Compiled CSS contains #c8a96a (gold), #b48f43 (gold-hover), #8a6d3b (gold-deep), text-gold-deep + border-gold-deep utilities, and the .calendly-overlay override (bg ink + z-index 2147483000). HTML renders the split: 22 text-gold-deep (light sections) + 41 text-gold (dark+fills); Hero "Creative Studio" label confirmed text-gold (onDark). Inline Calendly data-url still the real literal. No leftover openCalendlyPopup imports; no compile errors. NOT verifiable headlessly: hardware/gesture Back closing lightbox/menu/Calendly popup, the opaque Calendly overlay + header occlusion + safe-area, Calendly's X-close/reopen (no iframe leak), and the menu→popup handoff (req 17) — all need an on-device / real-browser check. The menu→popup history sequencing is the single most delicate interaction to verify on a phone.
- status: ✅ verified — 2026-07-24


## [Fix] Lightbox scroll-restore + mobile BOOK A CALL race + 15→30 min copy — 2026-07-24
- files: src/hooks/useBackToClose.ts (rewrite), src/components/ui/Lightbox.tsx, src/components/sections/Hero.tsx, src/components/sections/FinalCta.tsx, SKILL.md, CHANGELOG_work.md
- TASK1 diagnosis: Lightbox body-lock (position:fixed; top:-scrollY) already captured/restored scrollY, but (a) restored via legacy window.scrollTo(0,y) which obeys global html{scroll-behavior:smooth} → animated "jump" on close; (b) on Back, history.back()'s popstate + default history.scrollRestoration='auto' overrode the restore; (c) no scrollbar-width compensation → desktop sideways shift on open. Mobile menu does NOT use a body scroll lock (Header only tracks menuOpen + useBackToClose) → req5 N/A, nothing to fix there.
- TASK1 fix: restore now window.scrollTo({top:scrollY,left:0,behavior:'instant'}) (explicit instant overrides smooth); added scrollbar-width padding-right compensation on lock (guarded >0, no-op on mobile overlay scrollbars); useBackToClose sets history.scrollRestoration='manual' so the browser stops fighting the restore. All three close paths (Back/X/Esc) funnel through the same effect cleanup → identical behaviour.
- TASK2 diagnosis: CTAs already share one handler (useCalendly().openPopup); mobile CTA was wired (closes menu then openPopup). Real cause = history race: closing the mobile menu fires useBackToClose's history.back(), whose popstate is caught by the just-opened Calendly popup's own popstate listener → popup closes instantly. Desktop has no menu to close → no race → worked. (Not a script-load issue: that would be flaky on desktop too.)
- TASK2 fix: rewrote useBackToClose as a coordinated ref-counted hook — ONE shared history entry across overlapping overlays; the consuming history.back() is deferred to a microtask so a simultaneous open (menu→popup handoff) cancels it (order-independent; also StrictMode-safe). No stray popstate → no race. API unchanged, so Lightbox/Header/CalendlyProvider callers are untouched. Mobile CTA still closes menu then opens popup (req8) — now the popup stays open.
- TASK2 CTA matrix (paths, verified by code; live tap needs device): header desktop → CalendlyButton→openPopup ✓; header mobile menu → CalendlyButton onClick=closeMenu then openPopup, coordinator prevents race ✓; hero → CalendlyButton→openPopup ✓; pricing "Start with the Pilot" → CalendlyButton→openPopup ✓; final CTA → inline CalendlyInline embed (not a popup) — unaffected ✓.
- TASK3 copy 15→30 min: Hero CTA "Book a 15-min Call"→"Book a 30-min Call"; FinalCta H2 "Fifteen minutes."→"Thirty minutes."; SKILL.md 3 spots (Block2 CTA, Block9 H2, Calendly popup list). Full grep (15[ -]*min|15-minute|fifteen) now returns 0 in src+SKILL; no FAQ/metadata/alt/pricing mentions existed. No other copy changed.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>; HTML shows "Book a 30-min Call" + "Thirty minutes. Bring a product.", zero stale 15-min/fifteen copy; catalogue lightbox triggers render; no compile errors. NOT verifiable headlessly (need a real device/browser): the mobile scroll-position restore on lightbox close (instant, no jump), desktop no-sideways-shift on open, hardware Back closing lightbox, and mobile BOOK A CALL now opening + staying open through the menu→popup handoff. These are the interaction fixes — code/types verified, on-device confirmation still recommended.
- status: ✅ verified — 2026-07-24

## [Assets] ExclusiveModels casting board — real photos replace placeholders — 2026-07-24
- files: src/components/sections/ExclusiveModels.tsx, CHANGELOG_work.md
- req1: placeholders were HARDCODED in the component (local CANDIDATES const, Array.from → placehold.co URLs) — NOT a src/lib data file. Put the four real paths in that same local array (no new data structure). Added CASTING_W/H constants.
- req2/3: next/image with explicit width=1031 height=1280 (real intrinsic dims, no CLS). Grid/spacing/radius/card styling unchanged; tile stays overflow-hidden rounded, image stays h-auto w-full object-cover.
- req4: no separate "Candidate N" text element existed — the label was baked into the placeholder image + alt; removing the URLs removes it.
- req5: alt = "Casting candidate {n} — exclusive digital model" (consistent with Block 5 copy).
- req6: loading="lazy", no priority (below the fold).
- req7: all four files identical — 1031×1280 portrait, ratio 0.8055 (45–102 KB). No aspect-ratio differences → no silent crop, nothing to decide.
- req8: placehold.co now referenced ONLY by catalogues.ts HERO_IMAGE (hero) + next.config.mjs remotePattern. The hero is the sole remaining user → remotePattern KEPT (not removed).
- note: removed `unoptimized` here per this file's own TODO (real images arrived) → next/image optimizes these small below-fold thumbnails. Differs from the Work portfolio images, which kept unoptimized because that task had an explicit no-recompress rule; this task had none. Flag for alignment if desired.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>. All 4 casting imgs render via next/image (local /casting/0N.jpg through the optimizer), each with loading="lazy", width="1031" height="1280", and alt="Casting candidate N — exclusive digital model"; no "Candidate+" placeholder text; no casting preload link (not priority); the 4 files serve 200 image/jpeg directly; "Cast once. Yours forever." section intact; no compile errors. placehold.co now only in HERO_IMAGE + remotePattern (hero = sole user; remotePattern kept).
- status: ✅ verified — 2026-07-24

## [Hero] Hero visual is the Aibrium logo on the cream card — FINAL, no photo — 2026-07-27
- files: web/src/components/sections/Hero.tsx, web/src/lib/catalogues.ts, web/next.config.mjs, SKILL.md, CHANGELOG_work.md
- req1 (card + logo): there was no cream card element — the cream panel WAS the placeholder image (placehold.co/1400x1000/F7F3EC/C8A96A "Aibrium Hero") inside a bare `rounded-xl overflow-hidden` wrapper. Made that same wrapper the real card: added `bg-cream` + `aspect-[7/5]` (the placeholder's exact 1400×1000 ratio) so the panel keeps its identical footprint, radius and position → the two floating cards keep their exact offsets. Nothing else about the card changed (no new radius/shadow/border/colour).
- req2 (no CLS): next/image, `width={977} height={1000}` = real intrinsic dims. Centred via `flex items-center justify-center` + `p-12 lg:p-16`; image is `h-auto max-h-full w-auto max-w-full object-contain`, so the padded box caps it — the mark sits centred with clear air and never touches the card edges (renders ~50% of card width, not panel-filling).
- req3: DELIVERY / YOUR MODEL floating cards untouched — same absolute offsets (-left-6 top-10 / -right-4 bottom-10), same z-20 layer over the card, same copy + "A" avatar dot.
- req4: `priority` added (hero mark, loads immediately); alt = "Aibrium Studio". Removed `unoptimized` and its TODO — the asset is local now, so it goes through the Next image optimizer.
- req5 (mobile): visual block was already `hidden lg:block` from the 2026-07-23 deviation — KEPT as-is, not re-added. Below lg there is no card, no logo, no floating cards; the section starts straight with the text, and the mobile DELIVERY/YOUR MODEL facts block under the CTAs still carries both facts.
- req6: `HERO_IMAGE` (placehold.co URL) → `HERO_LOGO = '/hero-logo.png'` in catalogues.ts. Hero was the last placehold.co user in code → removed the remotePattern; next.config.mjs is now `{}` (all images local). "Aibrium Hero" text is gone repo-wide. placehold.co still appears in DOCS ONLY (README.mD, BUILD_PLAN.md, instructions.md, this CHANGELOG, SKILL.md asset-placeholder section) — historical/instructional prose, no runtime effect; left alone (task scoped SKILL.md edits to Block 2).
- req7: SKILL.md Block 2 rewritten — right column is now recorded as FINAL: logo on the cream card, no photograph, no hero.webp, do not reintroduce a placeholder. Mobile line reaffirmed (no card on mobile).
- trade-off flagged: `priority` + `hidden lg:block` means mobile preloads a 57 KB logo it never displays. Accepted per the explicit "mark it priority" instruction; reversible by dropping `priority`.
- ISSUE FLAGGED (not a code bug): hero-logo.png is RGB with NO alpha — the artwork sits on an opaque WHITE background. On the cream (#F7F3EC) card it therefore reads as a white rectangle inside the cream panel. Built cream as instructed ("do not restyle the card"). Fix is one line either way: card → `bg-white`, or supply a transparent-alpha PNG.
- verify: tsc --noEmit clean; npm run dev → GET / 200, one <h1>, zero dev-log errors/warnings. Rendered HTML: the hero <img> is alt="Aibrium Studio" width="977" height="1000" fetchPriority="high" with a matching <link rel=preload as=image> (priority active, no CLS), served through the Next optimizer (/_next/image?url=%2Fhero-logo.png → 200 image/png, 47 KB; raw file 200, 58 KB); card wrapper = `aspect-[7/5] items-center justify-center overflow-hidden rounded-xl bg-cream p-12 lg:p-16`; visual block still `relative hidden lg:block`; both floating-card facts present (also once each in the lg:hidden mobile facts block); zero "placehold"/"Aibrium Hero" strings in the HTML.
- verify (geometry, computed): at 1200px content width the card is 544×389 and the logo resolves to ~255×261 — height-capped by the p-16 inner box, ~64px air top/bottom and ~145px left/right. At the lg=1024 breakpoint it is 456×326 card / ~193×198 logo. Centred mark, never touching the card edges, at both ends of the desktop range.
- verify (limit): no headless browser in this environment, so confirmation is rendered-markup + computed-layout, not a screenshot. Eyeball recommended for the white-background issue flagged above.
- status: ✅ verified — 2026-07-27

## [Hero] Transparent logo replaces the opaque one; sized clear of the floating cards — 2026-07-28
- files: web/src/lib/catalogues.ts, web/src/components/sections/Hero.tsx, SKILL.md, CHANGELOG_work.md
- req1 (no white box): there was NO white-backing element in the code to remove — the card was already `bg-cream` with nothing between it and the image. The white rectangle WAS the PNG: hero-logo.png is RGB, no alpha, so its own opaque white background painted over the cream. `HERO_LOGO` → '/hero-logo-transparent.png' (RGBA, 4167×4167) removes the box-inside-a-box entirely. No wrapper/background/fixed box added or left behind; artwork sits straight on the cream fill.
- req2 (card unchanged): `aspect-[7/5] rounded-xl bg-cream overflow-hidden` + position/size all untouched — same footprint, colour, radius. (The card has no shadow to preserve; it never had one. The gold bloom behind it is unchanged.) Only the interior changed.
- req3 + req4 (centre + no overlap): root cause of the clipped wordmark — the two floating cards have FIXED heights (75px card A = py-4 32 + 11px label at lh 1.5 16.5 + mt-1 4 + 18px display leading-tight 22.5; 72.5px card B) at a fixed top-10 / bottom-10 inset, while the cream card is FLUID (456px→544px wide across lg→1200px+). Clear band between them = `cardH - 227`, i.e. only ~100px at lg. The old sizing came from card padding, which knows nothing about the floating cards, so the logo grew straight into card B. Fix: even `p-28` (112px all round — even padding as asked, standard Tailwind scale, no magic number), which caps the logo box at `cardH - 224`. Because the padding is in the SAME fixed px units as the cards' fixed heights, one value holds across the whole desktop range — no breakpoint juggling.
- req4 geometry (MEASURED, not estimated): decoded the served PNG's alpha channel — artwork bbox is the middle 62.9%×64.6% of the square frame, with 15.8% clear above it and 19.6% below, so the image BOX can exceed the visible mark and the cards sit over empty corners without touching artwork. (Planning estimate had the bottom margin at ~24%; the real 19.6% means the artwork reaches lower than assumed, so the figures below are the corrected ones.) Card A occupies y 40→115, card B y (cardH-112.5)→(cardH-40). At lg=1024 (card 456×325.7): box 101.7px, artwork y 128.1→193.8 vs card A ending 115 (13.1px clear) and card B starting 213.2 (19.4px clear). At 1200px+ (card 544×388.6): box 164.6px, artwork y 138.0→244.3 vs 115 and 276.1 (23.0px / 31.8px clear). Whole logo visible, wordmark included, at both ends of the range. Floating cards NOT moved — same corners, same -left-6 top-10 / -right-4 bottom-10 offsets, same copy.
- req5: next/image, `width={800} height={800}` — square, matching the square 4167×4167 source, so the box is reserved up front and nothing shifts. `priority` kept, alt "Aibrium Studio" kept.
- req6: desktop only — visual block still `relative hidden lg:block`, untouched. Not re-enabled on mobile.
- req7: web/public/hero-logo.png (the old opaque file) is now referenced NOWHERE in code or config — only in yesterday's CHANGELOG prose (historical record). NOT deleted, per instruction; user decides.
- req8: SKILL.md Block 2 updated — transparent logo centred on the cream card, desktop only, no white box, plus an explicit "do not shrink p-28" note with the reason, so a later phase does not reintroduce the clipping.
- trade-off flagged: sizing the logo to clear the cards (the user's stated preference) makes the mark modest — visible artwork ~104×106px inside a 544px card at full width, ~64×66px at lg. The lever for a bigger mark is moving the floating cards out of the band, not reducing the padding.
- verify: tsc --noEmit clean; npm run dev (restarted fresh) → GET / 200, one <h1>, zero dev-log errors/warnings. Rendered HTML: hero <img> src/srcSet = /_next/image?url=%2Fhero-logo-transparent.png, alt="Aibrium Studio", width="800" height="800", fetchPriority="high" + matching <link rel=preload as=image> (priority live, square box reserved → no CLS); card = `relative z-10 flex aspect-[7/5] items-center justify-center overflow-hidden rounded-xl bg-cream p-28`; visual block still `relative hidden lg:block` (desktop only, not re-enabled on mobile); floating cards still at `-left-6 top-10` and `-right-4 bottom-10`; zero references to the old hero-logo.png in the HTML.
- verify (alpha — the point of the whole task): raw file 200 image/png 148 KB; through the optimizer 200 image/png 25.9 KB at 828×828, colorType 6 = RGBA — the optimizer did NOT flatten alpha onto white, which would have reintroduced the box. Decoded that output: all four corners are [0,0,0,0] and 94.9% of pixels are fully transparent. No white/opaque backing behind the mark; the artwork sits directly on the cream card.
- verify (limit): still no headless browser here, so this is rendered-markup + decoded-pixel + computed-layout evidence, not a screenshot. The clearance figures follow from the info-cards' fixed heights (75 / 72.5px, derived from py-4 + line-heights) — if those heights are off, the margins move with them, so a quick eyeball at 1024px and at full width is still worth it.
- status: ✅ verified — 2026-07-28

## [Hero] Horizontal logo lockup — trimmed to a tight file, sized clear of the floating cards — 2026-07-28
- files: web/public/hero-logo-wide.png (NEW), web/src/lib/catalogues.ts, web/src/components/sections/Hero.tsx, SKILL.md, CHANGELOG_work.md
- req2 APPROACH (a) — TRIMMED. Decoded the alpha channel of the designer's hero-logo-transparent.png: 4167×4167 RGBA, 97.4% fully transparent, content bbox x 525→3735 / y 1327→2838 inclusive = 3211×1512, aspect 2.1237, margins 12.6% left / 10.4% right / 31.8% top / 31.9% bottom. This MATCHES the user's own measurement (525–3736 / 1327–2839 exclusive) exactly. Chose (a) over (b) because with the padded canvas the dead space still counts as layout box — visible artwork is only 36.3% of the box height, so it either renders tiny or needs negative margins to fake it, which also breaks the vertical centring. Trimming makes box == artwork, so object-contain centring is honest.
- req2 TRIM METHOD: no image library available in this environment (no sharp/PIL/ImageMagick), so wrote a pure-Node PNG decoder+encoder (zlib inflate, all 5 scanline filters, crop, re-deflate with CRC32 chunks) in the scratchpad. Output verified by re-decoding: 3211×1512 RGBA, content bbox now 0→3210 / 0→1511 (artwork touches all four edges = trim is exact), corners still [255,255,255,0], 90.6% transparent. Also eyeballed the decoded image — clean horizontal lockup, no encoder corruption. 135,508 → 62,521 bytes (-54%).
- req1: HERO_LOGO → '/hero-logo-wide.png' (the trimmed file). The designer's hero-logo-transparent.png is NOT overwritten — left intact as the source; catalogues.ts records the crop provenance in a comment.
- req3: cream card untouched — same `aspect-[7/5] rounded-xl bg-cream overflow-hidden`, same size/colour/radius, same gold bloom behind it, still present. (The card has no shadow to preserve; it never had one.) Logo never goes on the ink background.
- req4 + req5 (centring + no overlap): padding p-28 → p-32 (128px, even all round). The clear band between the floating cards is unchanged (`cardH - 227`: card A y 40→115, card B y cardH-112.5→cardH-40), but because the lockup is WIDE and the two cards sit at OPPOSITE corners (top-left / bottom-right), staying inside that band now clears both of them at any width — the horizontal extent no longer matters, which is exactly what the redraw was for. p-32 caps the logo at `cardH - 256` tall and it stays height-limited at both ends: at lg=1024 (card 456×325.7) logo 148.0×69.7 spanning y 128→197.7 (13.0px clear of card A, 15.5px of card B); at 1200px+ (card 544×388.6) logo 281.6×132.6 spanning y 128→260.6 (same 13.0 / 15.5px). Full lockup — logomark + AIBRIUM + STUDIO — visible and uncovered. Floating cards NOT moved: same corners, same -left-6 top-10 / -right-4 bottom-10, same copy.
- req4 balance: at 1200px+ the lockup is 51.8% of the card width with 131.2px of air each side and 128px above/below — near enough width-limited (281.6 wide vs 288 available) that both axes do similar work. At lg it is 32.5% of card width; that is the band limit, not a choice.
- req6: width={850} height={400} = 2.125:1 vs the file's real 2.1237:1 (0.06% error, sub-pixel at this size) — NOT 1:1. Correct box reserved up front → no layout shift, and the srcset candidates stay sane (passing the raw 3211 would have requested 3840px renditions for a ~282px slot).
- req7: desktop only — visual block still `relative hidden lg:block`, untouched, not re-enabled on mobile.
- req8 UNREFERENCED FILES (listed, NOT deleted): web/public/hero-logo-transparent_old.png (148 KB, the superseded vertical lockup) and web/public/hero-logo.png (59 KB, the original opaque no-alpha file) are referenced NOWHERE in code or config. web/public/hero-logo-transparent.png (136 KB, the designer's untrimmed source) is no longer LOADED either — it appears only in a provenance comment in catalogues.ts; worth keeping as the source for future re-trims. Only hero-logo-wide.png is live.
- req9: SKILL.md Block 2 updated — horizontal lockup, trimmed /hero-logo-wide.png, 850×400 never 1:1, p-32, desktop only, plus two "do not" notes (keep the file trimmed + do not shrink p-32) and explicit warnings off both dead files.
- verify: tsc --noEmit clean; npm run dev (RESTARTED, not hot-reloaded — see the WSL watcher note in the footer entry above) → GET / 200, one <h1>, zero dev-log errors. Rendered HTML: hero <img> src/srcSet = /_next/image?url=%2Fhero-logo-wide.png, alt="Aibrium Studio", width="850" height="400" (2.125:1, NOT 1:1), fetchPriority="high" + matching <link rel=preload as=image>; card = `relative z-10 flex aspect-[7/5] items-center justify-center overflow-hidden rounded-xl bg-cream p-32`; visual block still `relative hidden lg:block` (desktop only); floating cards still at `-left-6 top-10` / `-right-4 bottom-10`. Zero references to hero-logo.png, hero-logo-transparent.png or _old in the HTML.
- verify (padding actually applied): confirmed `.p-32 { padding: 8rem }` is present in the served layout.css and the old p-28 rule is gone — worth checking explicitly, because a missing utility would silently mean zero padding and the exact collision this task was fixing. (First grep for it reported MISSING; that was my own regex assuming minified single-line CSS, which the dev build is not — the rule was there all along.)
- verify (alpha survives the optimizer): raw file 200 image/png 62,521 bytes; through the optimizer 200 image/png 36,193 bytes at 1080×509 (aspect 2.122, matches), colorType 6 = RGBA, all four corners [0,0,0,0], 88.8% transparent, artwork bbox spans the full frame. No flattening onto white, no box behind the lockup, trim preserved end to end.
- verify (limit): still no headless browser here, so clearance is computed from the cards' fixed heights (75 / 72.5px) rather than screenshotted. The horizontal lockup makes this far less fragile than the vertical one — it only has to stay inside the vertical band, and the 13.0 / 15.5px margins hold at both ends of the desktop range — but an eyeball at 1024px and at full width is still worth it.
- status: ✅ verified — 2026-07-28

## [Type + Hero] Playfair Display replaces EB Garamond; hero visual shown on mobile (TRIAL) — 2026-07-28
- files: web/src/app/layout.tsx, web/tailwind.config.ts, web/src/components/sections/Hero.tsx, SKILL.md, CHANGELOG_work.md

TASK 1 — serif face → Playfair Display
- req1+req3: swapped through the TOKEN only. tailwind.config.ts `display: ["var(--font-playfair)", "Georgia", "serif"]`; all 28 `font-display` usages across 11 components inherit it — zero component font names edited. Montserrat (`font-label`) and Inter (`font-body`) untouched; <body className="font-body"> unchanged, so no Playfair anywhere in body text.
- req2: layout.tsx imports `Playfair_Display` from next/font/google, weight ["500","600"], subsets ["latin"], display:"swap", variable "--font-playfair", applied on <html> alongside montserrat/inter. The EB_Garamond import, its const and its variable are gone (grep: zero "garamond" left in code except two explanatory comments in Hero.tsx that cite it as the previous face).
- req4 OPTICAL — HONEST LIMIT: there is NO browser in this environment, so I could NOT do the visual check at 390 / 768 / 1440 that was asked for. Did not pretend otherwise. Made the minimum metrics-justified adjustment instead, both on the hero H1 only, both trivially revertible, and flagged for the user's eyes:
  (a) tracking -0.01em → -0.02em. The -0.01em existed because EB Garamond ran wider than Cormorant; Playfair runs wider still at display sizes.
  (b) line-height 1.05 → 1.08. Playfair's cap-height+descender span is ~0.98em vs EB Garamond's ~0.93em, so at 1.05 the gap between wrapped lines fell from ~0.12em to ~0.07em — ~40% less clearance. This H1 wraps (2 lines at 1440px, 3 at 390px), so it was the exposed case.
  Section H2 left at line-height 1.1 (~0.12em clearance, comfortable at that size). NO font sizes and NO type-scale values changed anywhere.
- req5: SKILL.md typography updated — display face is Playfair Display, CSS var --font-playfair, H1 row carries the new 1.08 / -0.02em with the reason, H2 + card-title rows renamed. Also updated the "Global layout" font-loading code block, which was still showing Cormorant_Garamond + Inter only (stale through two prior face swaps), and the Block 2 H1 line that said "Cormorant 600".
- STALE COPY REPORTED, NOT CHANGED: "Cormorant" still appears in SKILL.md lines 45 (Tech stack), 87 (setup note), 328 (Block 1 Header wordmark), 377 (Block 4), 401 (Block 6), 427 (Block 8), 655 (Build order) — all in sections outside the ones this task scoped. Line 328 matters most: it tells a later phase the Header wordmark is Cormorant. Flagged for the user.

TASK 2 — hero visual on mobile (reversible trial)
- req6+req7: visual block now renders at every width and `order-first` lifts it ABOVE the H1 on mobile; `lg:order-none` returns ordering to DOM flow at lg. Cream card, horizontal logo and BOTH floating cards all present on mobile — same content as desktop.
- CONFLICT FOUND + HANDLED: the mobile facts block under the CTAs exists only as a stand-in for a hidden visual. With the floating cards now visible on mobile it would print DELIVERY / YOUR MODEL a second time, so it is `hidden` for the trial. Reported to the user rather than silently deleted; it reverts with the same change.
- req8 SIZING: desktop `p-32` (128px) is impossible on mobile — at 390px the card is 342×244.3, so 128px padding gives the logo a NEGATIVE box. Card padding is now `px-[8%] py-[23.6%] lg:p-32`. Padding percentages resolve against the card's WIDTH and card height = width/1.4 (aspect-[7/5]), so 23.6% of width == 33% of height: the padding scales WITH the card and the lockup holds ~51% of card width at 390px AND 768px — the same proportion as desktop. At 390px: logo 176.1×82.9 at y 80.7→163.6; card A (shrunk) ends 71.3 → 9.5px clear; card B starts 174.3 → 10.7px clear. At 768px: logo 370.6×174.5, ~97px clear each side (floating cards keep fixed heights while the card grows, so margins only widen).
- req8 OVERFLOW: floating cards shrink below lg (`-left-2 top-4` / `-right-2 bottom-4`, px-3 py-2.5, gap-2, h-7 avatar, 10px labels, 13px/12px values). Kept SINGLE-LINE deliberately — letting them wrap would make them taller and squeeze the very band the logo sits in. Estimated widths at 390px: card A ~201px from x=16; card B ~274px with right edge at x=374 — both inside the 390px viewport. Independently of those glyph estimates, the section already carries `overflow-hidden`, so a document-level sideways scroll is structurally impossible.
- req9: DESKTOP UNCHANGED — every mobile value is overridden at `lg:` with the exact pre-trial value (lg:p-32, lg:-left-6 lg:top-10 lg:px-5 lg:py-4, lg:-right-4 lg:bottom-10 lg:gap-3, lg:h-9 lg:w-9, lg:text-[11px]/[18px]/[15px]/[16px]), and lg:order-none restores DOM ordering.
- req10: unchanged — width={850} height={400} (2.125:1, the real artwork ratio), priority, alt "Aibrium Studio".
- req11 THE SWITCH — TWO LINES, both marked `TRIAL` in Hero.tsx: (1) the visual block `<div className="relative order-first block lg:order-none">` — restore to `relative hidden lg:block`; (2) the facts block `<motion.div className="hidden">` — restore to `mt-10 flex flex-col gap-5 lg:hidden`. Nothing else needs touching; the responsive card/floating-card values are inert once the block is hidden again.
- req12: SKILL.md Block 2 carries a ⚠️ TRIAL bullet describing the mobile behaviour and naming both revert lines, placed ABOVE the original desktop-only bullet, which is kept intact and relabelled "pre-trial behaviour" so reverting has a spec to return to.
- verify: tsc --noEmit clean; npm run dev (RESTARTED, not hot-reloaded — WSL watcher) → GET / 200, exactly one <h1>, 45 font-display uses in the HTML, zero dev-log errors/warnings.
- verify TASK 1: served CSS defines `--font-playfair: '__Playfair_Display_3ef8f9', '__Playfair_Display_Fallback_3ef8f9'`, <html> carries the matching __variable_3ef8f9 class, "Playfair" appears 12× in the CSS, and "garamond" appears ZERO times in the CSS and ZERO times in the HTML. @font-face srcs resolve — sampled /_next/static/media/47f136985ef5b5cb-s.woff2 → 200 font/woff2 21,144 bytes. H1 renders `tracking-[-0.02em] ... [line-height:1.08]` with the clamp font-size untouched.
- verify TASK 2: visual wrapper = `relative order-first block lg:order-none`; card = `... bg-cream px-[8%] py-[23.6%] lg:p-32`; facts block = `hidden`; floating cards carry the full mobile+lg pairs. Confirmed the arbitrary utilities were actually GENERATED (padding-left/right:8% at CSS line 1268, padding-top/bottom:23.6% at 1312) — a silently-missing arbitrary utility would mean zero padding and an overlapping logo. Confirmed the CASCADE: `.lg\:p-32` sits at line 1745 INSIDE the @media (min-width:1024px) block opened at 1678, so it beats the base px/py on desktop regardless of class-string order. `.order-first{order:-9999}` and `.lg\:order-none{order:0}` both present; all six sampled lg: floating-card overrides present.
- verify LIMITS (two real ones): (1) req4's optical check at 390/768/1440 was NOT performed — no browser here. The tracking/leading changes are metrics-derived, not eyeballed; they are the two values most worth the user's own look, and each is a one-token revert. (2) mobile clearances and the floating-card widths at 390px are computed from font metrics and fixed paddings, not measured — the ~201px / ~274px card widths in particular are glyph-width estimates, now against Playfair rather than EB Garamond. The no-sideways-scroll guarantee does NOT depend on them (the section's overflow-hidden is structural), but whether the cards look tucked rather than cramped does. Worth a look at 390px.
- status: ✅ verified — 2026-07-28

## [Hero] Mobile hero-visual trial REVERTED — desktop-only again — 2026-07-28
- files: web/src/components/sections/Hero.tsx, SKILL.md, CHANGELOG_work.md
- req1+req2: hero visual block is `relative hidden lg:block` again — cream card, horizontal logo and BOTH floating cards hidden below lg. Mobile now starts straight with the H1, then the sub-paragraph, then the CTA buttons, then the two facts under them. `display:none` leaves no reserved box, so there is no empty space where the card was.
- req4 — THE LINES CHANGED BACK (two, not one; flagged as a matched pair when the trial was built): (1) web/src/components/sections/Hero.tsx:101 visual wrapper `relative order-first block lg:order-none` → `relative hidden lg:block` — this is the visibility switch, and `order-first`/`lg:order-none` going with it restores natural grid flow (text left / visual right at lg). (2) Hero.tsx:74 mobile facts block `hidden` → `mt-10 flex flex-col gap-5 lg:hidden`. The second is required, not optional: with the floating cards hidden again, the facts block is the only thing carrying DELIVERY / YOUR MODEL on mobile, and Block 2 mandates those facts must not disappear. Reverting only the first would silently drop them.
- ALSO REVERTED (judgement call, reported): the trial's mobile-only sizing, which is dead code once the block is hidden below lg. Collapsed each pair back to its pre-trial single value — card `px-[8%] py-[23.6%] lg:p-32` → `p-32`; card A `-left-2 top-4 px-3 py-2.5 lg:-left-6 lg:top-10 lg:px-5 lg:py-4` → `-left-6 top-10 px-5 py-4`; card B `-right-2 bottom-4 gap-2 px-3 py-2.5 lg:…` → `-right-4 bottom-10 gap-3 px-5 py-4`; avatar `h-7 w-7 text-[13px] lg:h-9 lg:w-9 lg:text-[15px]` → `h-9 w-9 text-[15px]`; labels `text-[10px] lg:text-[11px]` → `text-[11px]`; values `text-[13px] lg:text-[18px]` → `text-[18px]` and `text-[12px] lg:text-[16px]` → `text-[16px]`. Hero.tsx is now byte-equivalent to its pre-trial state apart from the Playfair H1 values and dated history comments.
- req3 DESKTOP UNCHANGED — provable, not assumed: in every collapsed pair the `lg:` variant was already the winning declaration at lg+, and the value kept IS that `lg:` value. Same computed styles at every width the visual is actually rendered at.
- req5: Playfair Display work untouched — layout.tsx still imports Playfair_Display with variable --font-playfair, tailwind.config.ts display token still `var(--font-playfair)`, and the H1 keeps `tracking-[-0.02em]` + `[line-height:1.08]`. Nothing in this task touched fonts.
- SITE HEADER NOT TOUCHED (explicit requirement): web/src/components/layout/Header.tsx mtime is still 07-24 11:42 — not opened, not edited. Only Hero.tsx changed in web/src.
- req6: SKILL.md Block 2 mobile bullet restored to a single desktop-only statement, now recording that the 2026-07-28 trial was reviewed and REJECTED, with "do not re-enable on mobile without being asked". The ⚠️ TRIAL bullet is gone.
- verify: tsc --noEmit clean; npm run dev (RESTARTED, not hot-reloaded — WSL watcher) → GET / 200, one <h1>, zero dev-log errors. Rendered HTML: visual wrapper is `relative hidden lg:block`, facts block is `mt-10 flex flex-col gap-5 lg:hidden`, card is `... bg-cream p-32`, floating cards are back at `-left-6 top-10 px-5 py-4` and `-right-4 bottom-10 gap-3 px-5 py-4`. Both facts ("Finished visuals every Friday", "never reused") appear twice — once in the lg:hidden mobile block, once in the desktop floating cards — so mobile keeps them.
- verify (trial fully gone): zero occurrences in the HTML of order-first, lg:order-none, px-[8%], py-[23.6%], `-left-2 top-4`, `-right-2 bottom-4`.
- verify (req5, Playfair untouched): served CSS still defines --font-playfair: '__Playfair_Display_3ef8f9'…, zero "garamond" in CSS and HTML, H1 still `tracking-[-0.02em] ... [line-height:1.08]` with the clamp size unchanged.
- verify (header untouched): <header> renders, all four nav anchors (#work #process #pricing #faq) present in both desktop and mobile menus; Header.tsx mtime unchanged at 07-24 11:42.
- verify (limit): no headless browser here, so this is rendered-markup verification, not a screenshot. What matters for this task is binary and confirmed in the markup — the visual is display:none below lg, so mobile cannot show a card or reserve space for one.
- status: ✅ verified — 2026-07-28
