
## [Footer] Copyright line simplified to "© 2026 Aibrium Studio" — 2026-07-28
- files: web/src/components/layout/Footer.tsx, SKILL.md, CHANGELOG_work.md
- req1: bottom line is now exactly `© 2026 Aibrium Studio`. Removed the legal entity («Mining capital LLC» SRL), the "t/a Aibrium Studio" trading-as phrase, "Chișinău, Republic of Moldova", and the Privacy Policy item.
- req2: nothing dangles — both `·` separators and the `{' '}` join disappeared with the text they joined; the `<a href="/privacy">` element is gone entirely. The wrapper `<div>` was KEPT deliberately: it carries the `border-t border-white/10`, `mt-14 pt-8`, `text-[13px]` and `text-[#B9B2A6]` and still holds the copyright line, so deleting it would have changed the layout rather than tidied it. No now-unused imports (the link was inline markup, not an import).
- req3 (REPORTED BEFORE DELETING): the Privacy Policy link was NOT a placeholder — it pointed to `/privacy`, a real Next.js route at web/src/app/privacy/page.tsx (5.5 KB: full policy, own <h1>, collection / third-parties / rights / contact sections). Only the footer <a> was removed; the page, the route and its sitemap.ts entry (`/privacy`, priority 0.3) are all untouched.
- req3 CONSEQUENCE FLAGGED: SKILL.md line 453 records that the privacy page is "Linked from the footer only", so it is now ORPHANED — reachable by direct URL and still crawlable via sitemap.ts, but no in-site link points to it. A footer privacy link is a common legal/compliance expectation. Reported to the user; left as instructed. (SKILL.md's /privacy section was NOT edited — out of the sections this task scoped.)
- req4: copy edit only. Footer grid, columns, wordmark, email/nav links, Book-a-call button, alignment, spacing, borders and colours all unchanged.
- req5 (grep report — NOTHING changed in these): the removed strings still appear outside the footer in three live spots — web/src/app/privacy/page.tsx:25 (policy intro, "Aibrium Studio («Mining capital LLC» SRL, trading as…)"), web/src/app/privacy/page.tsx:120-121 (contact block, entity + "Chișinău, Republic of Moldova."), and SKILL.md:664 ("Content facts" → Legal: entity · city/country). Plus CHANGELOG_work.md:56 (historical prose). NO hits in metadata, OpenGraph, JSON-LD/structured data, sitemap.ts or robots.ts — nothing machine-readable carried the legal entity, so nothing SEO-facing changed. Net effect: the legal entity is gone from the footer but still present in the privacy page body.
- req6: SKILL.md Block 10 bottom-line copy replaced with the single new line + a dated "do not restore" note, and a pointer that /privacy still exists and still carries the entity in its own copy.
- verify: tsc --noEmit clean; npm run dev → GET / 200 and GET /privacy 200, zero dev-log errors. Home footer bottom line renders exactly `<div class="mt-14 border-t border-white/10 pt-8 text-[13px] text-[#B9B2A6]">© 2026 Aibrium Studio</div>` — wrapper and all its classes unchanged, nothing after the text. Home HTML now contains ZERO occurrences of "Mining capital", "SRL", "Chi"(șinău), "Moldova", "Privacy Policy" and `href="/privacy"`. Rest of the footer intact: wordmark, "Creative studio for fashion…", hello@aibrium.com, office@aibriumstudio.com, nav anchors, Book a call button.
- verify (page not removed): /privacy still returns 200 with its own <h1> "Privacy Policy" and still carries the legal entity in its body; sitemap.xml still lists https://aibrium.com/privacy. Only the footer link went.
- verify (gotcha worth recording): the first verification pass returned the OLD footer text. Cause = the repo lives on /mnt/b, a Windows drive under WSL, where Next's file watcher does not reliably see changes — the dev server logged a recompile but kept serving the stale build. Restarting `npm run dev` picked the change up from disk. Verify against a RESTARTED dev server on this filesystem; a hot-reload result cannot be trusted here.
- status: ✅ verified — 2026-07-28
